{"kind": "fix_note", "source_label": "fix:gradle-kapt-error", "text": "# 修复笔记：Gradle KAPT 构建失败\n\n## 错误信息\n```\nFAILURE: Build failed with an exception.\n* What went wrong:\nExecution failed for task ':app:kaptDebugKotlin'.\n> A failure occurred while executing org.jetbrains.kotlin.gradle.internal.KaptExecution\n```\n\n## 根因分析\nKAPT (Kotlin Annotation Processing Tool) 处理失败，通常是因为：\n1. 注解处理器配置冲突\n2. 内存不足\n3. 增量编译缓存损坏\n\n## 解决方案\n\n### 方案 1：清理缓存\n```bash\n./gradlew clean\nrm -rf .gradle\nrm -rf app/build\n./gradlew build\n```\n\n### 方案 2：增加内存\n在 `gradle.properties` 中添加：\n```properties\norg.gradle.jvmargs=-Xmx4g -XX:MaxMetaspaceSize=1g\nkapt.use.worker.api=false\n```\n\n### 方案 3：禁用增量编译\n在 `gradle.properties` 中添加：\n```properties\nkapt.incremental.apt=false\n```\n\n## 预防措施\n- 定期清理构建缓存\n- 保持依赖版本一致\n- 使用 KSP 替代 KAPT（推荐）", "tags": ["fix", "gradle", "kapt", "build"], "error_signatures": ["Execution failed for task :app:kaptDebugKotlin", "KaptExecution failure", "KAPT annotation processing failed"]}
