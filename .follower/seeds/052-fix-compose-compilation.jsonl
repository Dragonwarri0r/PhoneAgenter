{"kind": "fix_note", "source_label": "fix:compose-compilation", "text": "# 修复笔记：Jetpack Compose 编译错误\n\n## 错误信息\n```\n@Composable invocations can only happen from the context of a @Composable function\n```\n\n## 根因分析\n在非 Composable 上下文中调用了 Composable 函数：\n1. 在 ViewModel 中直接调用 Composable\n2. 在回调中调用 Composable\n3. 在后台线程调用 Composable\n\n## 解决方案\n\n### 方案 1：使用 SideEffect\n```kotlin\nLaunchedEffect(key) {\n    // 在 Composable 上下文中执行副作用\n}\n```\n\n### 方案 2：提升状态\n```kotlin\n// 将状态提升到 ViewModel\nval state by viewModel.state.collectAsState()\n\n// 在 ViewModel 中处理逻辑\nviewModel.processAction(action)\n```\n\n### 方案 3：使用 DisposableEffect\n```kotlin\nDisposableEffect(key) {\n    // 初始化\n    onDispose {\n        // 清理\n    }\n}\n```\n\n## 常见场景\n\n### 场景 1：在点击事件中导航\n```kotlin\n// 错误\nButton(onClick = { navController.navigate(\"detail\") }) // OK\n\n// 正确\nButton(onClick = { viewModel.navigate(\"detail\") })\n// 在 ViewModel 中使用 Navigator\n```\n\n### 场景 2：在协程中更新 UI\n```kotlin\n// 错误\nscope.launch { text = \"Hello\" }\n\n// 正确\nLaunchedEffect(Unit) { text = \"Hello\" }\n```", "tags": ["fix", "compose", "ui", "compilation"], "error_signatures": ["@Composable invocations can only happen from the context of a @Composable function", "Composable function invocation", "Compose compiler error"]}
