{"kind": "fix_note", "source_label": "fix:room-migration", "text": "# 修复笔记：Room 数据库迁移错误\n\n## 错误信息\n```\njava.lang.IllegalStateException: Room cannot verify the data integrity.\nExpected schema hash: XXXX, Found: YYYY\n```\n\n## 根因分析\nRoom 数据库 schema 版本不匹配：\n1. 数据库 schema 变更但未提供迁移\n2. 迁移路径不完整\n3. schema 文件未同步\n\n## 解决方案\n\n### 方案 1：添加迁移\n```kotlin\nval MIGRATION_1_2 = object : Migration(1, 2) {\n    override fun migrate(database: SupportSQLiteDatabase) {\n        database.execSQL(\"ALTER TABLE users ADD COLUMN age INTEGER\")\n    }\n}\n```\n\n### 方案 2：Fallback 到重建（开发阶段）\n```kotlin\nRoom.databaseBuilder(context, AppDatabase::class.java, \"app.db\")\n    .fallbackToDestructiveMigration()\n    .build()\n```\n\n### 方案 3：导出 Schema\n在 `build.gradle.kts` 中：\n```kotlin\nksp {\n    arg(\"room.schemaLocation\", \"$projectDir/schemas\")\n}\n```\n\n## 参考文档\n- `docs/room-migration-constraints.md`\n\n## 预防措施\n- 每次修改 Entity 时同步更新 Migration\n- 提交 schema 文件到版本控制\n- 测试迁移路径", "tags": ["fix", "room", "database", "migration"], "error_signatures": ["Room cannot verify the data integrity", "IllegalStateException: Room", "Expected schema hash", "Migration failed"]}
